// Set up components
var grid = new Grid(4);
var state = new State();
var html = new Html();
var inputs = new Inputs();

// Constructing the game object
var game = new Game(grid, html, inputs, state);